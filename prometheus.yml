global:
  scrape_interval: 5s
  evaluation_interval: 5s


#rule_files:
#  - alert-rules.yml

scrape_configs:
  - job_name: "spring"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets:
        - 'host.docker.internal:8081'